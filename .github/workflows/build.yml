name: build-neon-ios-modifier-v2

on:
  push:
    branches:
      - "feature/neon-ios-modifier-v1"
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build arm64 iOS modifier dylib
        run: |
          chmod +x Scripts/build-ios.sh
          Scripts/build-ios.sh
      - name: Verify Mach-O and Chinese neon theme catalog
        run: |
          file build/NeonModifier.dylib
          otool -hv build/NeonModifier.dylib
          otool -L build/NeonModifier.dylib
          strings build/NeonModifier.dylib | grep -q "UnityFramework+0x1A2B3C"
          strings build/NeonModifier.dylib | grep -q "ZNNeonModifierThemeIndex"
          strings build/NeonModifier.dylib | grep -q "20"
          cat build/SHA256.txt
      - name: Upload dylib
        uses: actions/upload-artifact@v4
        with:
          name: NeonModifier-v0.2.0-Chinese20Styles-arm64
          path: |
            build/NeonModifier.dylib
            build/SHA256.txt
